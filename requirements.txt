# Web Framework (UI)
streamlit>=1.34.0

# Video Download and Processing
# yt-dlp>=2026.3.17
# opencv-python==4.9.0.80
# scenedetect[opencv]==0.6.3

# OCR and AI
# easyocr==1.7.1
# Note: PyTorch is required but should be installed manually to match your GPU (CUDA).
# For RTX 5060 (CUDA 13.2+), use:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu132

# PPT Generation
python-pptx==0.6.23

# Compatibility Constraints (Crucial for older AI libraries to avoid NumPy 2.0 crashes)
# numpy<2
# pillow<11

# PDF Translator Service
pdfplumber==0.11.0
deep-translator==1.11.4
python-docx==1.1.0
openai>=1.0.0

